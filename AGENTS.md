# AGENTS.md

## 项目目标

本项目为大学生创新创业训练计划中的氢燃烧器/燃烧室喷嘴排列参数化与仿真项目。

总体技术路线：

固定或参数化喷嘴参数
→ 定义燃烧室几何约束
→ 生成候选喷嘴排列
→ 自动进行几何合法性检查
→ 导出喷嘴坐标
→ 建立 Fluent CFD 模型
→ 对不同排列方案进行 CFD 对比
→ 根据几何与流动/燃烧性能指标筛选优选方案。

本仓库首先负责喷嘴排列生成、几何验证、评价指标计算、结果输出以及 CFD 前处理所需数据准备。

---

## 开发原则

1. 优先理解现有代码结构，再进行修改。
2. 尽量采用增量修改，不进行与当前任务无关的大规模重构。
3. 不得删除已有可正常工作的排列算法，除非任务明确要求。
4. 不得随意改变已有输入参数和 CSV 输出格式。
5. 如果确实需要修改公共接口，必须说明原因和影响范围。
6. 新功能应尽可能模块化，避免继续把所有逻辑堆积到单一脚本。
7. 算法应优先采用确定性的数学/参数化生成方法，而非无约束随机采样。
8. 不得为了让测试通过而降低几何约束或删除验证逻辑。
9. 不要修改与当前任务无关的文件。
10. 未经明确要求，不执行 git commit、git push、merge、rebase 或删除分支等 Git 操作。

---

## 喷嘴排列基本要求

每一种喷嘴排列算法至少应满足以下检查：

* 实际生成喷嘴数量等于目标数量；
* 所有喷嘴均位于允许的燃烧室几何区域内部；
* 喷嘴外缘不得越过燃烧室有效边界；
* 任意两个喷嘴之间满足规定的最小中心距；
* 不允许喷嘴几何重叠；
* 对要求对称的排列，应检查对称性；
* 参数发生变化后算法仍应能够正常生成合理结果。

不要仅通过图片人工判断排列是否合法。

---

## 参数化要求

尽量将以下参数从算法内部常量中解耦：

* 喷嘴数量 N；
* 喷嘴直径；
* 最小允许中心距；
* 燃烧室有效半径或几何边界；
* 边界安全距离；
* 排列类型；
* 与具体排列相关的角度、环数、行列数或其他参数。

避免仅针对 N=24 编写无法推广的硬编码算法。

如果某种文献排列本身只适用于特定数量，应在代码和文档中明确说明适用条件。

---

## 新排列算法要求

新增排列形式前：

1. 检查项目中已有算法和公共接口；
2. 尽量复用已有几何和验证函数；
3. 说明该排列的数学构造方式；
4. 如果排列来自论文或公开文献，在项目文档中记录对应来源；
5. 将文献中的“工程排列思想”和本项目中的“参数化实现方式”区分开，不得声称代码完全复现论文，除非确实如此。

每种新排列至少应提供一个可运行示例。

---

## 几何验证

几何验证模块应尽可能独立于具体排列算法。

建议至少能够输出：

* nozzle_count；
* boundary_valid；
* min_center_distance；
* spacing_valid；
* overlap_count；
* symmetry_error（适用时）；
* overall_valid。

如果验证失败，应尽可能明确指出失败原因，而不仅返回 False。

---

## 评价指标

在“满足几何约束”和“评价方案优劣”之间保持区别。

几何合法性属于硬约束。

用于排序不同合法方案的指标可以包括：

* 最小中心距；
* 中心距均匀性；
* 空间利用率；
* 径向分布均匀性；
* 周向分布均匀性；
* 对称性指标；
* 与边界之间的安全裕量。

CFD 性能指标属于后续物理评价层，不应仅根据几何指标直接宣称某种排列具有更好的燃烧性能。

---

## 输出要求

程序应尽量支持：

* PNG/SVG 等排列可视化；
* CSV 喷嘴坐标；
* 参数配置记录；
* 几何验证结果；
* 评价指标；
* case ID。

同一个 case 的配置、坐标、图片和验证结果应能够相互对应。

输出应尽可能支持后续 Fluent 建模和批量仿真。

---

## 可复现性

对于批量生成或对比实验：

* 保存完整输入参数；
* 保存排列类型；
* 保存计算得到的评价指标；
* 使用明确的 case ID；
* 如果使用随机过程，必须记录随机种子；
* 尽可能记录生成结果所对应的 Git 版本。

任何用于论文、结题材料或答辩的数据都应能够根据保存的参数重新生成。

---

## 测试要求

修改核心算法后至少检查：

1. 程序是否能正常运行；
2. 已有排列是否仍然正常；
3. 喷嘴数量；
4. 边界条件；
5. 最小中心距；
6. 重叠；
7. CSV 输出；
8. 图像输出。

如果仓库已有自动化测试，应运行全部相关测试。

新增重要算法时应同步增加测试。

---

## Codex 工作方式

对于复杂任务：

1. 先阅读相关代码；
2. 总结当前架构；
3. 给出修改计划；
4. 再开始修改；
5. 修改完成后运行测试和必要示例；
6. 检查 git diff；
7. 最后总结：

   * 修改了哪些文件；
   * 为什么这样修改；
   * 测试结果；
   * 生成了哪些示例；
   * 是否存在尚未解决的问题。

不要仅以“代码已经编写完成”作为任务完成标准。
